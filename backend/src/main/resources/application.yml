server:
  port: 8080

spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/campus_master?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai}
    username: ${DB_USERNAME:root}
    # ⚠️ 启动前请设置环境变量 DB_PASSWORD=<你的 MySQL 密码>，或在下方默认值中填入（仅限本地开发，勿提交到公开仓库）
    password: ${DB_PASSWORD:'Zdx962436510！'}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Asia/Shanghai

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    # 开发环境显示SQL日志，生产环境建议注释掉
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

jwt:
  secret: ${JWT_SECRET:campus_master_dev_secret_key_2026_local}
  expire: ${JWT_EXPIRE:86400000}

logging:
  level:
    com.example.campusmaster: debug
